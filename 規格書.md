# ShellPig's Shelf — 個人作品集網站 規格書

版本：v0.6
日期：2026-06-14
GitHub repo：https://github.com/shellpig/the-shelf
線上網址（部署後）：https://shellpig.github.io/the-shelf/

---

## 1. 專案概述

一個展示個人作品的靜態網站。首頁頂部是大標題「ShellPig's Shelf」，並有簡短自我介紹與聯絡連結；下方為卡片牆，每張卡片含一張圖、標題、簡述與技術棧。點卡片進入該作品的獨立細節頁，細節頁顯示名稱、簡述、多張截圖與該專案的 README 內容，以及相關連結（網頁類作品含線上試玩連結）。

作品內容以資料檔（`projects.json`）為唯一來源，透過本機後台表單（`admin.html`）新增/編輯（圖片可拖曳上傳），不需手寫 HTML。網站以純靜態形式部署於 GitHub Pages。

---

## 2. 核心需求

| 編號 | 需求 | 說明 |
|---|---|---|
| R1 | 首頁大標 | 頁面最上方主標題：**ShellPig's Shelf** |
| R2 | 自我介紹區 | 大標下方簡短自我介紹 + 聯絡連結（GitHub 等） |
| R3 | 卡片牆 | 一張張卡片，每張：一張圖 + 標題 + 簡述（約2行）+ 技術棧 |
| R4 | 響應式排版 | 桌面 3 欄 / 平板 2 欄 / 手機 1 欄 |
| R5 | 細節頁 | 點卡進入獨立細節頁（方案 A：每作品一頁，靠 `?id=` 區分）；內容：名稱、簡述、多張圖、README 內容 |
| R6 | 作品連結 | 細節頁顯示相關連結；網頁類作品含線上試玩連結 |
| R7 | 後台表單 | 本機表單新增/編輯/刪除/排序作品，圖片可拖曳上傳，存回 `projects.json`，不手寫 HTML |
| R8 | 中文 | 介面與內容皆中文 |
| R9 | 免費部署 | 純靜態，部署於 GitHub Pages |

---

## 3. 技術選型

- **前端**：原生 HTML + CSS + JavaScript，不使用框架。
- **資料**：單一 `projects.json`（純文字、git 可版控）。
- **Markdown 渲染**：細節頁的 README 內容以 markdown 形式存於資料中，前端用輕量函式庫（如 marked.js）渲染為 HTML。
- **後台存檔與圖片**：瀏覽器 File System Access API，直接讀寫本機 `projects.json`；圖片以拖曳上傳，存入 `images/`。無需伺服器。
- **排版**：CSS Grid 控制三欄/兩欄/單欄響應式。
- **部署**：GitHub Pages（純靜態）。

> 不採用框架、資料庫、後端伺服器——以目前 3 個作品、單人維護的規模，純靜態最簡單、免費、零維護。

---

## 3.1 視覺設計（方向 C：紙感書架）

整體走「紙感書架」溫潤調性：暖米色底、磚紅點綴、襯線標題、柔和邊框，呼應 Shelf（架子）命名。

### 配色

| 用途 | 色碼 | 說明 |
|---|---|---|
| 頁面底色 | `#f6f1e7` | 暖米色 |
| 卡片底色 | `#fffdf7` | 近白暖色 |
| 主要文字 | `#3a3225` | 深褐 |
| 次要文字 | `#8a7c64` | 暖灰褐 |
| 邊框 | `#e6dcc7` / `#e0d6c2` | 柔和米邊 |
| 強調色（磚紅） | `#993c1d` | 連結、技術標籤、hover 重點 |
| 佔位圖框 | 底 `#faf6ec`、虛線邊 `#d8cbb0` | 無圖時的封面佔位 |

> 須兼顧深色模式則另議；本版以淺色暖調為唯一主題。

### 字體（中文，可商用免費）

- 內文：思源黑體 / Noto Sans TC。
- 標題：思源宋體 / Noto Serif TC（襯線，帶書卷感）。
- 透過 Google Fonts 或自備字檔載入；實作時定案載入方式。

### 元件樣式

- **卡片**：暖白底、柔和米色全邊框、圓角 8px；hover 輕微浮起或邊框轉磚紅。
- **技術標籤**：磚紅字 + 淺米邊框的小膠囊。
- **連結**：磚紅色。

---

## 4. 檔案結構

```
the-shelf/
├─ index.html        首頁：大標 + 自我介紹 + 卡片牆
├─ detail.html       細節頁模板（靠 ?id= 顯示對應作品）
├─ admin.html        本機後台：表單新增/編輯作品（圖片拖曳上傳）
├─ projects.json     所有作品資料（唯一內容來源）
├─ site.json         站台設定（大標、自我介紹、聯絡連結）— 可選，或併入 index
├─ css/
│  └─ style.css
├─ js/
│  ├─ app.js         讀 json、渲染卡片
│  ├─ detail.js      渲染細節頁、markdown
│  └─ admin.js       後台表單邏輯、讀寫 json、拖曳上傳
├─ lib/
│  ├─ marked.min.js   markdown 渲染（輕量）
│  └─ cropper.min.js  圖片裁切（後台用）+ cropper.min.css
├─ images/           作品圖片
└─ 規格書.md
```

---

## 5. 資料模型（`projects.json`）

頂層為作品陣列，顯示順序即陣列順序。

```json
[
  {
    "id": "warboard-theater",
    "title": "Warboard Theater 戰局劇場",
    "summary": "在瀏覽器中執行的戰棋戰場劇場，載入戰役腳本後自動演繹戰局。純前端、開啟即玩。",
    "tech": ["JavaScript", "HTML", "CSS"],
    "cover": "images/warboard-cover.png",
    "images": ["images/warboard-1.png", "images/warboard-2.png"],
    "readme": "## 戰局劇場\n\n（此處為 README 的 markdown 內容，於細節頁渲染顯示）",
    "links": [
      { "label": "線上試玩", "url": "https://shellpig.github.io/Warboard-Theater/" },
      { "label": "GitHub", "url": "https://github.com/shellpig/Warboard-Theater" }
    ]
  }
]
```

### 欄位定義

| 欄位 | 型別 | 必填 | 說明 |
|---|---|---|---|
| `id` | string | ✅ | 唯一識別碼，英數與連字號；作為 `detail.html?id=` 參數，需唯一 |
| `title` | string | ✅ | 作品標題（卡片與細節頁標題） |
| `summary` | string | ✅ | 簡述（約2行）；卡片與細節頁共用 |
| `tech` | string[] | ✅ | 技術棧標籤，卡片上顯示 |
| `cover` | string | ✅ | 卡片封面圖路徑。無圖時用佔位外框（見 6.2） |
| `images` | string[] | 否 | 細節頁圖庫（多張截圖） |
| `readme` | string | 否 | 細節頁顯示的 README 內容（markdown） |
| `links` | array | 否 | 連結清單；每項 `{ label, url }`。無則不顯示連結區 |

---

## 5.1 圖片規格

| 用途 | 比例 | 尺寸 | 處理方式 |
|---|---|---|---|
| 卡片封面 `cover` | 固定 **16:9** | 輸出 **1280×720** | 後台拖入後裁切（鎖 16:9），輸出統一尺寸存入 `images/` |
| 細節頁圖庫 `images` | 不限（保留原圖） | 不限 | 只限制顯示最大寬度，依原比例排列 |

- 裁切元件：Cropper.js（輕量，隨站附於 `lib/`）。
- 顯示端封面一律加 `object-fit: cover` 作為保險，避免未裁好的圖跑版。

---

## 6. 頁面規格

### 6.1 首頁 `index.html`

- **大標區**：頁面頂部主標題「ShellPig's Shelf」。
- **自我介紹區**：大標下方一段簡短自我介紹 + 聯絡連結（GitHub 個人頁等）。內容來源見 §7（待作者提供）。
- **卡片牆**：載入時讀 `projects.json`，依序為每筆資料渲染一張卡片。
- **卡片內容**：
  - 上：封面圖（`cover`），等比例填滿卡片上半，外有邊框。
  - 下：標題（`title`）+ 簡述（`summary`，約2行）+ 技術棧（`tech`，以標籤呈現）。
- **互動**：整張卡片可點，連到 `detail.html?id={id}`；hover 有輕微提示（浮起或邊框變化）。

### 6.2 卡片圖片

- 有圖：顯示 `cover`，外加邊框。
- 無圖（佔位）：固定比例的灰色外框佔位區（可含標題首字或圖示），維持版面整齊。目前三作品皆無現成圖，初版先全部佔位。

### 6.3 細節頁 `detail.html`

由網址參數取得 `id`（如 `detail.html?id=warboard-theater`），從 `projects.json` 找出該筆資料，顯示：

1. **名稱**（`title`）
2. **簡述**（`summary`）
3. **圖庫**（`images`，多張截圖；無則顯示封面或佔位）
4. **README 內容**（`readme`，markdown 渲染為 HTML）
5. **連結區**（`links`，逐個連結；無則不顯示）
6. **返回首頁**連結

- **找不到 id**：顯示「查無此作品」並提供返回首頁連結。

### 6.4 響應式排版（R4）

以 CSS Grid 實作：

| 裝置 | 寬度（約） | 卡片欄數 |
|---|---|---|
| 桌面 | ≥ 960px | 3 欄 |
| 平板 | 600–960px | 2 欄 |
| 手機 | < 600px | 1 欄（逐張直排） |

---

## 7. 自我介紹 / 聯絡（R2）

首頁大標下方顯示：
- 一段簡短自我介紹文字（**待作者提供**）。
- 聯絡連結：GitHub 個人頁（https://github.com/shellpig）、Email（shellpig@gmail.com）。

實作上可存於 `site.json` 或寫入 `index.html`，於實作時定案。

---

## 8. 後台 `admin.html`（本機工具，一併上傳 GitHub）

僅供作者本機使用；檔案隨站台上傳，但訪客不會用到其功能。

### 8.1 介面

- **新增/編輯表單**：欄位對應資料模型——標題、簡述、技術棧（可增減多個標籤）、封面圖、細節圖庫、README 內容、連結（可動態增減多組 label+url）。
- **作品清單**：列出目前所有作品，每項可「編輯／刪除／調整順序」。

### 8.2 行為

1. 開啟後讀取本機 `projects.json`（File System Access API 選取檔案/資料夾）。
2. **圖片拖曳上傳**：
   - 封面：拖入後開啟裁切框（鎖 16:9），調整後輸出 1280×720 存入 `images/`，路徑寫入 `cover`。
   - 圖庫：拖入後依原比例存入 `images/`，路徑加入 `images` 陣列。
3. 表單送出 → 更新記憶體中的作品陣列。
4. 儲存 → 寫回 `projects.json`。
5. `id` 由標題自動產生（轉小寫、空白轉連字號），衝突時提示。

### 8.3 限制（已知並接受）

- 後台只能在作者本機、支援 File System Access API 的瀏覽器（Chrome/Edge）使用。
- 編輯後需作者自行 `git push` 才會上線，非即時。
- 此限制僅影響「作者編輯」，不影響訪客瀏覽。

---

## 9. 部署流程

1. 本機以 `admin.html` 編輯作品 → 拖曳上傳圖片 → 儲存 `projects.json`。
2. `git commit` + `git push` 至 https://github.com/shellpig/the-shelf。
3. GitHub Pages 自動發佈。
4. 訪客瀏覽：https://shellpig.github.io/the-shelf/

---

## 10. 初始內容（四個作品，依序：FactorHammer → Warboard Theater → After The Model → Sparkote）

**① FactorHammer**
- summary：台股／美股量化研究與回測工具，本機離線執行、不做實單交易。內建技術指標、雙回測引擎，並可選用 AI 輔助分析。
- tech：`Python · FastAPI · DuckDB · Next.js · React · Tailwind`
- links：GitHub repo

**② Warboard Theater 戰局劇場**
- summary：在瀏覽器中執行的戰棋戰場劇場，載入戰役腳本後自動演繹戰局。純前端、開啟即玩，無需安裝。
- tech：`JavaScript · HTML · CSS`
- links：線上試玩（https://shellpig.github.io/Warboard-Theater/）+ GitHub repo

**③ After The Model**
- summary：設定在 2030 年的 2D 橫向賽博龐克探索遊戲；扮演 AI 重塑後城市裡的底層小人物，接零工、解謎、找回被抹除的記憶。
- tech：`Godot 4 · GDScript · Python`
- links：GitHub repo（＋ Windows 下載，可選）

**④ Sparkote**（排最後）
- summary：由現實任務驅動的療癒探索遊戲。把現實中完成的自我照顧小任務化為能量，驅動郵差萊拉在迷霧群島間探索送信，讓世界一塊塊變得完整。
- tech：`Godot 4 · GDScript`
- links：線上試玩（https://shellpig.github.io/Sparkote/）+ GitHub repo

> 四作品皆無現成卡片圖，初版一律用佔位外框。各作品的 README 內容（細節頁用）待匯入。

---

## 11. 非目標 / 未來擴充

**本版不做：** 多使用者、登入帳號、線上即時編輯、後端伺服器、資料庫、標籤分類搜尋篩選、複雜動畫。

**未來可擴充：** 作品量大時改用靜態產生器或 Git-based CMS（方案 2）；分類／標籤／篩選；多語系；自訂網域。

---

## 12. 已定案決策

- 大標：ShellPig's Shelf
- repo：the-shelf；網址 https://shellpig.github.io/the-shelf/
- 後台方案：方案 1（本機表單 + File System Access API，純靜態）
- 細節頁：方案 A（每作品獨立頁，`?id=` 區分）
- 卡片內容：標題 + 簡述（約2行）+ 技術棧
- 細節頁內容：名稱 + 簡述 + 圖庫 + README 內容
- 圖片：後台拖曳上傳；封面固定 16:9、輸出 1280×720、可裁切（Cropper.js）；圖庫不限比例
- admin.html：一併上傳 GitHub
- 響應式：桌面3 / 平板2 / 手機1
- 含自我介紹 + 聯絡連結（GitHub + Email：shellpig@gmail.com）
- 作品共 4 件，順序：FactorHammer → Warboard Theater → After The Model → Sparkote
- 視覺方向：C 紙感書架（暖米底、磚紅點綴、襯線標題；見 §3.1）
- favicon / 小圖示：暫不做

---

## 13. 待確認事項（TODO）

1. ~~視覺調性 / 配色~~ ✅ 已定：方向 C 紙感書架（見 §3.1）。
2. 自我介紹文字內容（作者提供）。
3. ~~聯絡連結~~ ✅ 已定：GitHub + Email（shellpig@gmail.com）。
4. 各作品 README 內容如何匯入細節頁（由 README 複製為 markdown 存入資料）。
5. 四作品的封面圖與截圖（作者日後補上，初版用佔位）。
6. ~~Sparkote 簡述~~ ✅ 已補（見 §10 ④）。
7. ~~Sparkote 線上試玩網址~~ ✅ 已確認 https://shellpig.github.io/Sparkote/。
